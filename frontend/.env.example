# API Gateway URL (Single Entry Point for REST API & WebSocket)
VITE_API_GATEWAY_URL=http://localhost:3000

# WebSocket Configuration:
# - WebSocket connections now go through API Gateway
# - Gateway automatically detects namespace and routes to correct service
# - No separate service URLs needed for WebSocket anymore
# 
# How it works:
# - Chat: io(VITE_API_GATEWAY_URL + '/chat') → Gateway → Chat Service
# - Notification: io(VITE_API_GATEWAY_URL + '/notifications') → Gateway → Notification Service
# - REST API: axios(VITE_API_GATEWAY_URL + '/auth', '/users', '/conversations', etc) → Gateway → Services

# For production:
# VITE_API_GATEWAY_URL=https://your-api-gateway.onrender.com
# WebSocket and REST will automatically use this URL through the unified gateway

# Architecture:
#   - REST API & WebSocket → API Gateway (port 3000/3000) → Services
#   - Gateway detects connection type and routes appropriately
#   - Namespace-aware WebSocket routing ensures correct service connection
